# Foundations live CLI walkthrough contract

Authority: `contract-token-cache-cli-live-v1` in
`content/production/decision-log.md`; bounded scope in
`content/production/token-cache-cli-motion/plan.md`. Integration baseline
`cf1eeff` includes the authoritative owner edits at `7ac8b56`.

The original integration inserted only standalone slide 1 as live slide 14,
exact title **Same conversation. Different reuse.**, two minutes. In the
current 24-slide manifest it is slide 17: the three approved plain-English
prediction slides are 13–15, **How your message shapes the reply** is slide 16,
and the former 15–21 remain unchanged at 18–24.
**A miss is not always deletion.** remains standalone only.

Reuse `token-cache-cli-motion/source.md`, first-slide notes and teaching,
`components/CacheWalkthrough.vue`, `sequence.mjs`, and the exact approved
artwork/provenance bytes. The standalone source's no-live-allocation statement
describes its original standalone scope; this addendum authorizes only the
first slide's live allocation. No new external claims or pricing verification:
all rates/counts are illustrative, not actual Copilot pricing or telemetry.
The receipt is a separate synthetic example, not the next ledger turn.

Default full-canvas layout intentionally retains the accepted 960×540 native
composition, Play/Replay, finite 60-second motion, reduced-motion final state,
integer accounting, and leave/reentry reset. A thin live wrapper imports the
same implementation; scoped component styles prevent auto-discovery leakage.
Vite imports the existing standalone public artwork copy for both production
bases; no additional integration raster copy, regeneration, promotion, or font
acquisition is needed. Mona Sans remains
requested with Segoe UI/Arial fallback; exact Mona appearance is not established.

On this slide only, in landscape play mode, the wrapper restores Slidev's
native navigation hover/focus visibility. At rest the footer is unobstructed;
hovering or focusing the lower-left toolbar temporarily overlays it. Moving
away and leaving toolbar focus restores the clear footer. The owner's
persistent navigation behavior remains unchanged on every other slide and in
portrait. No shared navigation styles or components are modified.

## Reconciled live inventory and timing

`slide-manifest.md` governs all 24 exact title rows. Existing walkthrough bytes
and notes are retained. Visible instruction is 61 minutes; mission is 45;
visible allocation is 106. Intended instruction remains 77; the temporary gap
is 77 − 61 = 16, or 122 − 106 = 16. The declared 122-minute module is
unchanged. The two-minute walkthrough includes its one-minute motion; no
separate media minutes.

## Existing asset declaration

`assets/images/foundations/cli-controlled-action.png` and `.png.json`;
build input `token-cache-cli-motion/public/cli-controlled-action.png` and
`.png.json` are unchanged byte-identical approved copies. Provenance prompt:
`media/prompts/images/cli-controlled-action-v3.txt`. Reused on live slides 7
and 17 and standalone slide 1. No new acceptance or publication claim.
